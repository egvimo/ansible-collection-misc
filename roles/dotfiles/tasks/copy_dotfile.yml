---
- name: "[{{ _dotfile }}] Get dotfile configuration"
  set_fact:
    _dotfile_config: "{{ dotfiles_configuration[_dotfile] }}"

- name: "[{{ _dotfile }}] Include OS specific tasks"
  include_tasks: "{{ ansible_os_family | lower }}.yml"
